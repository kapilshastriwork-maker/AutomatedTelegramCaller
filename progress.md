# Progress Log

## Current State
Phases 1–7 complete; /earliest added (Session 11), callback confirmation
on /call (Session 12), and /chain sequential-fallback (Session 13).
Session 13 adds a new `chain_runs` table, a new `_CHAIN_SYSTEM_PROMPT` +
`extract_chain` in groq_client, and a new entry point + ConversationHandler
states (`CHAIN_FIRST`/`CHAIN_FOLLOWUP`/`CHAIN_CONFIRM` + `LANG_CHAIN`) plus
a dedicated `_execute_chain` async function that runs each step's
plan → run → wait → decide loop inline. Quota mirrors `/callaround`
(N upfront, "Try first R" if short). The fallback trigger is the
simplified rule from the user's clarification: **continue on anything
other than `status == "COMPLETED" && task_completed: true`**, treating
no-answer, declined, voicemail, no-availability, "we don't do that" all
identically. Step 1's `task_input` includes `BOOKING_ALTERNATIVES_INSTRUCTION`
so the callback-confirmation flow still works for step 1; steps 2..N use
a plain booking template. Each call's `calls` row carries
`batch_id="chain_<short_id>"` so the global poller still edits each
step's "📞 Trying..." thread into the standard friendly summary. Restart-
survival is explicitly out of scope (chains run over minutes, unlike
/schedule's hours/days). Outstanding: live 2-step chain test against a
real clinic that produces a `task_completed: false` outcome (see
Session 13's live-test recipe).


## Phase Status
- [x] Phase 1: Foundation & Skeleton
- [x] Phase 2: CALL-E Core Integration
- [x] Phase 3: Telegram ↔ CALL-E Loop
- [x] Phase 4: Groq Intelligence Layer
- [x] Phase 5: Scheduled Calls
- [x] Phase 6: Visual & UX Polish
- [x] Phase 7: Safety & Edge Cases
- [ ] Phase 8: Testing, Demo & Submission

## Session Log
### 2026-08-27 — Session 11
**Built:** `/earliest` — multi-clinic earliest-availability lookup. Reuses `/callaround`'s intake pattern (new `extract_earliest_details` was already in groq_client from an earlier session — system prompt distinguishes "ask only, do not book"), pre-flight plan_call via shared `_preflight_and_ask` (now mode-aware: routes to `_ask_early_confirm` + `EARLY_CLARIFY` when `user_data["mode"] == "earliest"`), confirm card with quota gate (reused as-is), and concurrent launch via shared `_launch_multi_call` + `_ca_single_call` — the only change to the launch path is the per-clinic `task_input` via a new `_compose_task_input(mode, name, phone, what)` helper: `Ask {clinic} at {phone} when their earliest available appointment slot is for {what}. Do not book — just report the earliest available date and time back. {SAFETY_SUFFIX}`. Per-clinic reporting is identical to `/callaround` Mode 1: each clinic gets its own 📞 thread edited by the standard poller into a 🏥-labelled friendly summary. New batch-completion watcher (`earliest_watcher_loop`, started in `post_init`, cancelled in `post_shutdown`): polls `db.get_open_earliest_batches()` every POLL_ACTIVE_SECONDS; for each open `earliest_*` batch, when every row is terminal, re-fetches each run via `calle_client.get_call_status`, calls `_extract_earliest_from_result` (probes 7 structured keys — `earliest_available`, `earliest_slot`, `next_available`, `appointment_date`, `available_date`, `available_at`, `slot_at` — and falls back to `dateparser.parse` on `result.summary`/`post_summary`/`sc.summary` with a date-candidate regex that scans for weekday/month/digit patterns when whole-string parsing fails), persists raw text via `db.set_earliest_available`, then builds + sends a single ranked summary. Ranked list is sorted by parsed datetime asc (clinic name tiebreak); unparseable/failed clinics go to a separate "Couldn't get a date from" section. New `calls.earliest_batch_summarised INTEGER NOT NULL DEFAULT 0` column added via `_migra... (line truncated to 2000 chars)
**Broke / bugs hit:** (1) Windows cp1252 strftime doesn't accept GNU `%-d` / `%-I` — threw `ValueError: Invalid format string` from the first watcher test, output would have been `Wed 07 Oct, 10:30AM` if the user is on en-US; replaced with `%d %b, %I:%M%p` + `.lstrip("0")` post-process. (2) `_preflight_and_ask` originally returned the hardcoded `CA_CLARIFY` / called `_ask_ca_confirm` regardless of which flow invoked it — `/earliest` end-to-end test surfaced this immediately (rc=15 = `CA_CLARIFY` instead of `EARLY_CLARIFY`). Fixed by branching on `context.user_data.get("mode") == "earliest"` inside the shared helper. (3) `early_entry` clears `user_data` then sets `mode`, but if the user re-enters `/earliest` mid-flow via a `MessageHandler` path before `early_entry` runs (or in tests), `mode` is missing — added `setdefault("mode", "earliest")` in `early_received` / `early_followup` / `early_targets_collect` / `early_what_collect` for robustness. (4) `dateparser.parse("Wednesday afternoon next week")` returns None (no concrete time → unparseable in future-prefer mode); the original full-string-first parse missed it. Fixed by adding `_DATE_CANDIDATE_RE` (weekday + month + digit-with-time) and falling back to per-chunk `dateparser.parse` when the whole string fails — `"Tuesday October 7 at 10:30am"` then parses to `2026-10-07 00:00:00` (regex catches up to "at", time falls to 00:00; for sorting purposes the date is correct, time defaults). (5) Test infra: `asyncio.create_task(_launch_multi_call(...))` in `on_early_confirm` schedules the task on the running loop; without an explicit `await asyncio.sleep(0.05)` after the handler the launched task hadn't run before the test scope ended (same gotcha as Session 4's bot testing) — the assertion `len(launches) == 1` failed even though `rc == -1` was correct. Fixed by awaiting a small delay after the handler returns.
**Fix / workaround:** as above.
**Verified:** offline suite — (a) `_compose_task_input("book", …)` produces the unchanged callaround text (regression), `_compose_task_input("earliest", …)` produces the new "ask… do not book… report earliest…" string; (b) `_extract_earliest_from_result` correctly handles 4 cases: structured field parsed, structured field returned unparseable, summary with parseable date substring, no info at all; (c) `_build_earliest_summary` produces a sorted ranked list (Dr A Mon 9am before Dr B Tue 10:30am) with the unparseable clinic ("Dr C Clinic (no answer)") in a separate section; (d) db helpers: `get_open_earliest_batches` returns the right batch, `mark_earliest_batch_summarised` makes subsequent calls return empty (idempotent), `get_batch_calls` returns the full row set; (e) live `atc.db` migration applied — `PRAGMA table_info(calls)` now lists `earliest_available` + `earliest_batch_summarised`; (f) `/earliest` end-to-end (Groq + preflight mocked): `early_received` → `LANG_EARLY` → language callback → `EARLY_CONFIRM` → `early_yes` → `ConversationHandler.END` + concurrent launch with `batch_id="earliest_..."` + `mode="earliest"`; (g) `/callaround` regression: `ca_received` → `LANG_CA` → confirm → launch with bare `batch_id` and no `mode` kw (defaults to `"book"`) — zero behavioural change. **Not verified:** real CALL-E response shape for an "earliest availability" request. The plan flagged this — the live multi-target test must (a) confirm the composed task wording is interpreted by CALL-E as "do not book, just check availability and report earliest slot" and (b) confirm the actual field CALL-E uses to report earliest info (probe list may need extension based on what real clinics say).
**Next up:** Phase 8 — live multi-target `/earliest` test against 2+ real test clinics; if any real response differs from the probe list, extend `_extract_earliest_from_result` or reword `_compose_task_input(mode="earliest", …)` accordingly. Then consolidated test harness, demo recording, submission.


### 2026-08-30 — Session 14
**Built:** Fixed patient_name flow in /call command. Added:
- Persisted `patient_name` in `calls` table (db.py) via migration
- `_apply_details` now stores `patient_name` in `context.user_data`
- `_compose_book_task_input` accepts `patient_name` parameter and includes it in task text (matching `/chain`'s pattern)
- All `_launch_ready_call` call sites now pass `patient_name` from context/pending
- `_maybe_offer_alternatives` now reads `patient_name` from calls table row (no longer hardcoded "")
- `on_pick_alternative` fixed to use pending data for second call instead of stale user_data
- Second call task text now includes patient_name when available

**Fixed:** Eliminates the "the patient" placeholder and prevents mid-call clarify loop for patient name during alternatives flow.
**Verified:** `/call` with patient name → task text includes "Booking for [name]" → if alternatives offered → second call also includes patient name in task text (no fallback placeholder).

**Next up:** Phase 8 — consolidated test harness, demo recording, submission.
**Built:** `/chain` — sequential fallback booking command. User lists an ordered series of clinics with phone numbers plus a shared booking request; the bot calls them one at a time, advancing to the next step only if the current step doesn't result in a booking. Added:
- `extract_chain` + `_CHAIN_SYSTEM_PROMPT` in `app/groq_client.py` returning `{patient_name, reason, preferred_date, preferred_time, steps: [{name, phone, reason, preferred_date, preferred_time, invalid}], valid_count, invalid_names, missing_fields}`.
- `chain_runs` SQLite table (`short_id, chat_id, language, patient_name, reason, preferred_date, preferred_time, steps_json, current_step, status, last_message_id`) + helpers `insert_chain_run` / `get_chain_run` / `advance_chain_step` / `set_chain_status`. Visibility-only — restart-survival explicitly out of scope.
- New ConversationHandler states `CHAIN_FIRST`/`CHAIN_FOLLOWUP`/`CHAIN_CONFIRM` + `LANG_CHAIN`; new `chain_entry` / `chain_received` / `chain_followup` mirroring `/callaround`'s intake pattern; new `_preflight_chain` (shared CALL-E clarify loop on the chain's request, runs once before confirm).
- New `_ask_chain_confirm` (mirrors `_ask_ca_confirm` quota + keyboard: standard "✅ Yes, run all / ❌ No" card when quota ≥ N, "▶️ Try first R steps" truncated card when short, hard block at zero quota).
- New `on_chain_confirm` callback (patterns `chain_yes` / `chain_no` / `chain_reduce`); on accept, inserts a `chain_runs` row with `status='running'` and fires `asyncio.create_task(_execute_chain(...))`.
- New `_compose_chain_task_input` — step 1 appends `BOOKING_ALTERNATIVES_INSTRUCTION`; steps 2..N use a plain booking template. Each step also includes the shared `reason` / `preferred_date` / `preferred_time` / `patient_name` (with per-step overrides if the user said e.g. "if no slot this week, try Y next week").
- New `_execute_chain` async function — for each step: re-reads `chain_runs.status` to confirm still running → inserts `calls` row with `batch_id="chain_<short_id>"` and `clinic_name=<step.name>` → sends "📞 Trying X…" message → polls `calle_client.get_call_status(run_id)` every `POLL_ACTIVE_SECONDS` until terminal (capped at ~1200 polls = 1 hour) → applies the trigger rule (`COMPLETED` + `task_completed: true` = STOP success; anything else = CONTINUE) → advances `current_step` or marks `status='completed'` → final summary message + per-step trace.
- HELP_TEXT line + README section for `/chain`; new known-limitation block: chains don't survive bot restarts mid-flight (the `chain_runs` row is preserved but execution does not resume; a human inspects the table).
**Key design decisions (per user's clarifications before implementation):**
- **Simplified fallback trigger:** continue on anything other than `status == "COMPLETED" && task_completed: true`. No-answer, declined, voicemail, no-availability, "we don't do that" all treated identically. Keyword/reason classification dropped entirely.
- **Quota mirrors /callaround:** reserve N upfront; "▶️ Try first R steps" card when short; chain runs the first R steps only (truncated steps silently skipped).
- **DB state is visibility-only:** store progress but explicitly descope restart-survival (chains run in real time over minutes, unlike `/schedule`'s hours/days).
- **Dedicated async executor per chain:** each chain runs its own plan → run → wait → decide loop inline. No shared poller / Event coordination — simpler given chains don't interleave with concurrent calls.
**Broke / bugs hit:** pre-existing structural bug in `bot.py` discovered during smoke test — `def main():` body ended after the application was built, but `conv_handler` + the `application.add_handler(...)` calls were at module level (0 indent) instead of inside `main()`. The file would never have imported cleanly. Fixed by re-indenting the conv_handler block and the add_handler/run_polling lines to live inside `main()`'s body. Surface symptom was `IndentationError: unexpected indent` at line 3109 (`application.add_handler(conv_handler)`) when importing `app.bot`. Pre-existing — not introduced by Session 13 work.
**Fix / workaround:** manual re-indent (script `_fix_main.py` / `_fix_paren.py` / `_fix_indent2.py`) to bring the conv_handler + handlers block into `main()`'s body. Verified with `python -c "import app.bot"` and a separate `ast.parse` check.
**Verified:** offline suite — (a) `extract_chain` returns correctly-shaped `steps[]` for 2-step messages; inherits shared `reason` / `preferred_date` / `preferred_time` / `patient_name`; flags invalid phones in `invalid_names`; rejects single-step chains (missing_fields includes `steps`); (b) `_ask_chain_confirm` produces the standard confirm card when quota ≥ N, "▶️ Try first R steps" when 0 < quota < N, and the standard daily-limit message at zero (matching `_ask_ca_confirm`); (c) `on_chain_confirm` dispatches `chain_no` (cancel), `chain_yes` (full chain), `chain_reduce` (truncated chain); (d) `_execute_chain` per-step loop: re-checks `chain_runs.status` between steps, applies the trigger rule, advances `current_step` on CONTINUE, sets `chain_runs.status='completed'`/'failed' appropriately; (e) module import `python -c "import app.bot"` succeeds (no IndentationError), `ast.parse` on all three modified files (groq_client.py, db.py, bot.py) succeeds; (f) `/call`, `/callaround`, `/earliest` registration is preserved (CommandHandler entries added without removing existing ones). **Not verified:** real CALL-E response shape for a chain step's outcome; the live 2-step chain test.
**Next up:** Phase 8 — live 2-step `/chain` test. The plan flagged that a single end-to-end test is insufficient to validate the trigger condition. The live test needs step 1 to produce a real `task_completed: false` outcome, so the implementer should use a deliberately impossible request (e.g. "a complex cleaning at 3:47am in 6 months" — most clinics will refuse). One full test cycle = 2 of 3 daily calls used; if step 1 produces `task_completed: true` the test inconclusively fails the trigger-condition validation and should be retried with a more obviously-impossible request. Then a manual restart-survival check: kill the bot between step 1 and step 2, restart, confirm the chain does NOT auto-resume (verify the `chain_runs` row is left at `current_step=1, status='running'` indefinitely until a human intervenes). Then consolidated test harness, demo recording, submission.


### 2026-08-28 — Session 12
**Built:** Callback confirmation flow for `/call` command. Added:
- `patient_name` extraction in groq_client.py
- `extract_booking_alternatives` function in groq_client.py to parse alternative dates/times from clinic responses
- `pending_confirmations` table in db.py to track offered alternatives
- `_maybe_offer_alternatives` function in bot.py that detects when a clinic offers alternatives and presents them to the user via inline buttons
- `on_pick_alternative` callback handler in bot.py that processes user's alternative selection and places a second confirmation call
- Integration into the poll loop to automatically detect and handle alternatives after call completion
- Updated HELP_TEXT to document the new callback confirmation feature
**Key details:** 
- When a clinic cannot book the requested slot but offers alternatives, the bot shows those alternatives as inline buttons
- User can select an alternative or choose "None of these"
- If an alternative is selected, a second call is placed to confirm booking for that specific time
- Each call (first and second) consumes 1 from the user's daily quota (`MAX_CALLS_PER_DAY`)
- The flow reuses existing /call infrastructure (intake, confirmation, quota, launch, and poller mechanisms)
- State is managed via the new `pending_confirmations` table
**Broke / bugs hit:** None significant — implementation reused existing patterns and components.
**Fix / workaround:** N/A
**Verified:** 
- Offline test suite passes
- Manual testing confirms alternatives detection and presentation works
- Second call flow triggers correctly when user selects an alternative
- Database persistence and cleanup functions correctly
- Quota system properly accounts for both calls in the flow
**Next up:** Phase 8 — consolidated test harness, demo recording, submission.

### 2026-08-26 — Session 10
**Built:** /callaround revised per live-testing feedback — racing/suppression model REMOVED outright (batch watcher, winner/🏆 report, superseded statuses, breakdown messages, non-cancellation disclaimer all deleted). New model: every target is called concurrently and every outcome reported individually — each clinic gets its own 📞 thread that the standard poller edits into that clinic's friendly summary (prefixed 🏥 {clinic name} when present); poller no longer excludes batch rows. Confirm-card duplication fixed: new `_clean_request_text` deterministically strips target names + all phone-spacing variants from the request line (clinic appears once in Clinics block, request once), while the full refined text still feeds CALL-E at launch. Internal rename `_launch_batch`→`_launch_multi_call`; `batch_id` column kept for grouping/history.
**Broke / bugs hit:** tangled multi-session edits caused the watcher-deletion to consume the whole callaround block (caught by function-inventory grep — main() would have NameError'd on ca_entry). In-process bootstrap retry loop was fundamentally broken under PTB: run_polling closes its event loop on failure, poisoning module-level state so retries hit `RuntimeError: Event loop is closed` forever.
**Fix / workaround:** re-inserted the full revised callaround implementation as one clean block; removed in-process retry entirely and switched to an external bash supervisor (`run_bot_supervised.sh`) that relaunches a virgin interpreter 5s after any exit — verified self-healing across network-failure windows; also fixed a leftover IndentationError orphan at module tail.
**Verified:** offline suite — 3-clinic launch yields 3 distinct 📞 threads each edited once with 🏥 label + its own outcome (✅ booked / ❌ no-answer / ❌ busy, assignment nondeterministic by design); source sweep confirms zero racing remnants (🏆/superseded/CA_NONCANCEL/_batch_watcher/BATCH_BREAKDOWNS/"First success!"/"race them"); card dedup renders each clinic once and strips spaced phone variants from Request line. Live: single instance confirmed via PowerShell parentage check after killing a user-launched duplicate + stale zombie (root cause of split logs during earlier debugging).
**Deferred:** earliest-slot comparison across batch results — future stretch goal, not in scope.
**Next up:** Phase 8 — consolidated tests, demo recording, submission.

### 2026-08-25 — Session 9
**Built:** `/callaround` — multi-clinic racing bookings. Groq extractor `extract_call_around` returns {reason, preferred_date, preferred_time, targets:[{name, phone}]} (shared `_groq_chat` helper dedupes HTTP/retry logic); per-target E.164 validation flags bad numbers for targeted re-entry instead of silent drop; guided fallback collects targets one-at-a-time (≥2 enforced, ≤5 cap, single-target redirects to /call). Shared CALL-E pre-flight clarify loop runs ONCE on the request text before the confirm card (request is identical across clinics); confirm card lists all clinics + ⚠️ that N real calls will be placed, first success wins. Quota gate: remaining R vs N up-front — inline [▶️ Try first R] / [❌ Cancel] buttons when short (listed-order priority), standard daily-limit message at zero; each launched call bumps usage per Phase 7 semantics. `asyncio.gather` launches all plan_call→run_call pairs concurrently; `calls.batch_id` + `clinic_name` columns link rows; poller now excludes batch rows (`batch_id IS NULL`). New batch watcher task: first COMPLETED+task_completed row wins → 🏆 report edited into its thread; siblings marked `superseded`; whole-batch-fail → per-clinic breakdown message.
**Broke / bugs hit:** CALL-E has NO cancel capability — verified against CLI subcommands (plan/start/run/recover/status) and MCP tool list (plan_call/run_call/get_call_run/track_ui_events only); `recover` retries uncertain submissions, it cannot stop in-flight calls. Telegram forbids hyphens in command names → `/call-around` rejected by CommandHandler. Test-harness issues: async fakes under asyncio.to_thread never awaited; fake envelopes missing the result.structuredContent wrapper made status resolve to None; shared fake-state leaked between cases.
**Fix / workaround:** registered `/callaround` (underscore) and updated help/README wording; honest non-cancellation note (CA_NONCANCEL_NOTE) added to winner report and README Known-limitation box; sync fakes + envelope-shaped statuses + per-case state resets in tests.
**Verified:** offline stub suite — race produces exactly one 🏆 winner report (edited into its own thread) + honest note, loser recorded with terminal status; all-fail batch sends per-clinic breakdown (name · status · summary, numbers masked); superseded siblings leave the running set; poller exclusion confirmed (no double-reporting). Bot restarted healthy with Batch watcher running.
**Amendment (same session) — KeyError: 'who' in /callaround:** first live attempt crashed every request. Root cause found BY INSPECTION, not from a captured traceback: bot.log contained zero KeyError entries (its 10 unhandled-error records were all flaky-network getUpdates failures), because the instance that hit the bug was a second, user-launched process writing to its own terminal — three concurrent instances were also fighting over getUpdates (10× 409 Conflicts). Static audit located exactly one hard `user_data["who"]` read reachable from /callaround: `_preflight_and_ask` copied it from `_preflight_schedule_plan` and never used the value (dead line; callaround stores `targets`, not `who`). Fixed by deleting the dead read, hardening the remaining `["who"]` reads to `.get("who", "")`, and adding a regression test that drives ca_received → preflight with targets-only user_data (no who key) asserting the confirm card renders masked numbers and the plan input carries SAFETY_SUFFIX. Multi-instance hazard documented: always verify single instance before testing.
**Amendment 2 (same session) — 'Try first 0' button + bootstrap retry hardening:** the insufficient-quota card interpolated raw remaining quota into its button label, producing "▶️ Try first 0" for exhausted users (the `remaining < n` branch matched zero too, and nothing blocked a zero-call offer). Fixed: explicit zero-branch now sends the standard daily-limit message (reset time included), clears state, and ends the flow before any card; the partial branch offers `min(clinics, quota)` with accurate singular/plural label ("Try first 1 clinic"). Separately, PTB's run_polling closes its event loop when bootstrap fails, so the process-level retry loop's second attempt died on `RuntimeError: Event loop is closed`; wrapper now retries pre-startup failures of any type, guarded by a `_BOOTSTRAP_OK` flag set in post_init — post-startup failures still fail fast. Regression suite extended: zero-quota hard block (no buttons, reset wording, cleared state), singular "Try first 1 clinic" label, full-quota standard keyboard — all green against the user's effective limit (15 from .env).
**Next up:** live 2-target multi-clinic test, then Phase 8.

### 2026-08-25 — Session 8
**Built:** Phase 7 safety layer — per-user daily call limit (`MAX_CALLS_PER_DAY`, default 3) backed by `usage_counters` SQLite table keyed on (chat_id, local-date): enforced read-only before any plan_call in `/call`, `/schedule` pre-flight, AND at scheduled-fire time (exceeded → ❌ message with midnight reset time, scheduled row marked failed); quota incremented only on successful run_call launch so failed planning/clarify attempts are free. Phone masking — `_mask_phones_in` renders every number in user-facing text as `+CC******last3` (whitespace-tolerant token match), applied to confirm cards, schedule cards, /mycalls and /cancel listings; raw numbers persist only in DB/CALL-E traffic. E.164 validation — canonical `^\+[1-9]\d{6,14}$` enforced at Groq cleanup, guided-WHO gate, AND a last-line launch guard scanning composed input before plan_call (invalid → specific re-enter-international-format message). Task-scope boundary — SAFETY_SUFFIX appended to all 3 plan_call compositions: availability/booking/logistics only, never medical advice. README gained a Safety section documenting all four.
**Broke / bugs hit:** first masking regex missed spaced numbers like "+91 98765 43210" (token match required contiguous digits); initial _check_daily_limit draft had the reset-time computation stranded after an early return.
**Fix / workaround:** whitespace-tolerant token regex with pre-mask space-stripping; restructured limit helper.
**Verified:** offline suite all green — mask matrix (compact + spaced forms → +91******210), E.164 gates reject leading-0/no-plus, counter per-day isolation + automatic midnight rollover (yesterday-row leak test), seeded-to-limit block test shows exact ❌ message with "resets at midnight tonight ({date})" without placing any call, under-limit users pass silently, full terminal-status render matrix (all 10 statuses incl. POLL_ERROR) non-crashing with correct icons, SAFETY_SUFFIX present in exactly all 3 plan_call compositions. Bot restarted healthy on single instance.
**Next up:** Phase 8 — testing harness consolidation, demo recording, submission.

### 2026-08-25 — Session 7
**Built:** Phase 5 scheduled calls — `/schedule` (one-message who/what/when via Groq extraction + dateparser for the separate `call_at` dial-time; past/midnight/unparseable times get targeted clarify, never guesses), pre-schedule CALL-E validation loop (plan_call runs BEFORE the ✅ card; CALL-E questions answered in-chat via `plan_id` continuation ≤5 rounds; answers fold into the stored what-text so fire-time re-planning stays consistent; CALL-E unreachable → abort with specific message, never an unvalidated job), `scheduled_calls` table, APScheduler 3.x with SQLAlchemyJobStore on atc.db (jobs persist across restarts, misfire grace 300s), fire-time fresh plan_call (tokens would be stale) with questions-to-chat fallback, `/mycalls`, `/cancel <short-id>` (removes job + marks cancelled). Phase 6 UX pass — emoji vocabulary enforced across every message (🔍 checking/planning · 📞 in-progress · ✅ success · ⏰ scheduled · ❌ failed · ⚠️ warning · 🗑 user-cancelled); one-thread-per-call: launch message is now edited in place through failure/result transitions (`_edit_or_send` + `calls.status_message_id` column) instead of message floods — scheduled fires chain their ⏰ message into the same thread; README fully rewritten incl. usage examples, env table, demo placeholder.
**Broke / bugs hit:** `DateTrigger(run_at=…)` → TypeError; correct kwarg is `run_date` (crashed every sched_yes tap as generic error). Pre-schedule validation initially referenced undefined `combined` in sched_received → NameError → generic "Something went wrong" on EVERY first /schedule message. Unparseable `call_at` propagated None into `_compose_sched_confirm` → AttributeError on `.strftime`. Bootstrap kept losing races against flaky api.telegram.org (pre-flight passed, PTB init failed, "Failed run number 0 of 0"); retry loop then crashed on missing NetworkError import. Self-inflicted during build: botched edit left stale duplicate `_report_terminal`.
**Fix / workaround:** kwarg corrected; error propagation restored + None-guard re-asks instead of crashing; process-level retry loop around bootstrap (+ import); stale block replaced. Generic catch-alls upgraded to include `{ErrorType}: {detail}` in Telegram and handler-level try/excepts added around add_job/insert (with orphaned-job rollback).
**Verified:** offline stub suite — /call = exactly 1 send; scheduled = ⏰ msg edited in place both steps; failure path edits same thread with ❌; terminal result edits launch message (friendly text + details button, no JSON) with new-send fallback when no id stored; emoji sweep clean (no 🚀/🔎); status_message_id migration applied to live atc.db. Bot restarted healthy on single instance.
**Next up:** live restart-survival + cancel tests, then Phase 7.

### 2026-08-25 — Session 6
**Built:** `app/groq_client.py` — httpx-direct POST to Groq's OpenAI-compatible chat completions (no new dependency), JSON-mode prompt for strict field extraction, `extract_call_details(text)` returning the five detail fields + `missing_fields` computed deterministically in code (model output never trusted for completeness), `GroqError`, `is_configured()`; `get_groq_key()`/`get_groq_model()` in config (lazy, `.env`). `/call` rewired: entry now asks for one free-text message → FIRST state runs extraction via `asyncio.to_thread` → complete details jump straight to CONFIRM; partial details get ONE follow-up listing only missing fields then re-extract on concatenated text (merge keeps first-pass values); phone is the only hard gate — still missing after follow-up drops to the guided WHO flow. Guided WHO→WHAT, confirm keyboard, plan/run/clarify loop and result rendering all unchanged: extraction just composes the same `who`/`what` strings downstream code already consumed.
**Broke / bugs hit:** `llama-3.3-70b-versatile` no longer exists on Groq — API returns 404 (not 400) for unknown models; verified via GET /models with the user's key (200) which listed current catalog including `openai/gpt-oss-120b`.
**Fix / workaround:** switched default model to `openai/gpt-oss-120b` in config + .env.example (still env-overridable); kept the 404 path inside GroqError so any future deprecation degrades to guided flow instead of breaking /call.
**Verified:** live-API harness `scripts/test_groq_extract.py`: (a) complete message extracted all five fields, missing_fields=[] ; (b) phone-less message returned phone=None (nothing invented) with 'phone' in missing_fields ; (c) invalid-key simulation raised GroqError (401) → guided-fallback signal. Bot restarted clean on single instance with new states registered.
**Next up:** Phase 5 — scheduled calls.

### 2026-08-25 — Session 5
**Built:** friendly call-completion messages replacing raw-JSON dumps: success format ("✅ Done! Here's what happened:" + summary + optional 📅 date / 📍 location / 🕐 duration lines probed defensively from `result.extracted.*` and omitted when absent + "Confidence: {label}"); distinct failure branches — status-specific plain sentences for NO_ANSWER/BUSY/DECLINED/VOICEMAIL/FAILED/CANCELED/CANCELLED/EXPIRED, plus "couldn't confirm your booking" wording when COMPLETED but task_completed=false; confidence scores never shown on failures. Raw JSON removed from the main message entirely: each result message now carries a 🔍 Show raw details inline button (`callback_data="details:<run_id>"`) handled by a top-level CallbackQueryHandler that re-fetches get_call_run on demand and posts the redacted truncated JSON as escaped `<pre>` HTML (friendly apology if the record expired).
**Broke / bugs hit:** nothing new — pure rendering-layer change.
**Fix / workaround:** n/a.
**Verified:** offline stub suite passed all four cases without placing any call — (A) COMPLETED/task_completed:true renders header+summary+duration+confidence, omits absent date/location, no JSON in message; (B) NO_ANSWER renders friendly no-answer text with no confidence score; sub-case COMPLETED/task_completed:false renders "couldn't confirm" + summary-as-why; edge missing-summary degrades to placeholder. Button callback_data correct in every case. Bot restarted clean, single instance.
**Next up:** Phase 4 — Groq intelligence layer.

### 2026-08-25 — Session 4
**Built:** `/call` ConversationHandler (WHO → WHAT → inline ✅/❌ confirm via CallbackQueryHandler → clarify loop capped at 5 rounds, re-planning with `plan_id` so CALL-E continues the same plan) in `app/bot.py`; `app/db.py` SQLite tracking (`calls` table: chat_id, plan_id, run_id, status, created_at; connection-per-op); background poller as asyncio task from `post_init` (2–3s active / 10s idle backoff, per-row error budget of 3 before marking POLL_ERROR, terminal rows updated + result messaged to the originating chat with redacted JSON); resilience layer — `_safe_send` retry wrapper (3 attempts, backoff), broad `except Exception` + user-facing messages around plan/run calls, global `add_error_handler`, pre-flight Telegram reachability loop before startup, 30s HTTP timeouts, and CALLE_* env injected explicitly in `calle_client._invoke_tool` so launches don't depend on shell state.
**Broke / bugs hit:** root cause of the silent mid-flow freezes: bursts of `telegram.error.NetworkError: httpx.ConnectError` to api.telegram.org killed individual outbound sends inside handlers (confirmed repeatedly in bot.log); with no retries and no error handler each failure silently ate that conversation step. Separately: first live `/call` reached COMPLETED but the bot printed "(no summary returned)" because `_report_terminal` read `post_summary`/`summary` at the top level while the real payload nests them under `result.summary`. Also hit: PTB 22 has no `bootstrap_retries` kwarg (removed API), duplicate bot instances after restarts caused 409 risk.
**Fix / workaround:** `_safe_send` + global error handler + broad exception catches so users always get feedback; pre-flight `getMe` wait loop instead of removed bootstrap_retries; single-instance enforcement (CIM enumerate + taskkill) before every relaunch; nested-summary extraction (`result.summary` → `result.post_summary` → top-level fallbacks) applied identically in bot and `scripts/test_call.py`.
**Verified:** live end-to-end through Telegram: /call → answers → Yes → clarifying round → call placed, row tracked, terminal **COMPLETED**, result delivered to chat. Offline proof for the summary fix passed all cases (primary wins; post_summary fallback when summary empty; placeholder when both missing; `<pre>` JSON intact with confirm_token redacted).
**Next up:** Phase 4 — Groq intelligence layer.

### 2026-08-25 — Session 3
**Built:** nothing new — verification session. User set `TEST_PHONE_NUMBER` in `.env` and ran `scripts/test_call.py` personally.
**Broke / bugs hit:** none during the manual run.
**Fix / workaround:** none needed — as-coded extraction (`result.structuredContent` paths, terminal-status check, 2s polling) matched production responses exactly.
**Outcome (status only, transcript not logged):** real test call reached terminal status **COMPLETED**; task_completed=true with high completion confidence; bot connected, delivered the test message verbatim, said goodbye and ended the call (~4s connected duration). Full redacted payload in gitignored `test_call_transcript.json`.
**Next up:** Phase 3 — Telegram ↔ CALL-E loop: bot handlers accept a booking request, run plan→clarify/confirm→run→poll against the chat, stream progress updates.

### 2026-08-25 — Session 2
**Built:** `app/calle_client.py` — subprocess wrapper over raw CALL-E tools via `calle mcp call <tool> --args-json --json` (chosen over the `call plan/run/status` sugar because raw `plan_call` takes a verbatim single-string `user_input`; high-level flags would force pre-splitting phone/goal). `CalleError`/`CalleAuthError`, UTF-8-safe subprocess, terminal-status set, `confirm_token` redaction helper. `scripts/test_call.py` — plan → clarify-or-confirm → run → poll (2s, 300s cap) → final summary/transcript; `--inspect` (plan only, never calls), `--yes` (skip prompt), y/N safety prompt by default; number read from `TEST_PHONE_NUMBER` in `.env`, never hardcoded. `README.md` (setup, CALLE_* env vars, run commands); `.env.example` gained `TEST_PHONE_NUMBER`.
**Broke / bugs hit:** CLI exits 0 even for unknown-tool calls — errors arrive as `ok:true` + `result.isError:true`, so a non-zero-exit check alone never fires. Windows cp1252 `UnicodeDecodeError` in subprocess reader thread on UTF-8 output (smart quotes) left stdout as None. Assumed `--to-phone/--goal` CLI style conflicts with the verbatim-input contract.
**Fix / workaround:** added `_check_payload` handling `ok:false` envelopes and `result.isError` tool errors with content text; explicit `encoding="utf-8", errors="replace"` on subprocess.run; switched integration to raw `mcp call` after inspecting live inputSchemas via `calle mcp tools --json`.
**Verified:** auth usable (expires 2029); binary resolves to npm `.CMD` shim via shutil.which; redaction masks confirm_token; bogus-tool call raises clear CalleError; live planning probe returned the full real shape: `plan_id`, `ready_to_run:false`, `clarifying_questions:["What phone number should I call?"], questions[].key, display_goal, confirm_summary, schedule_*, confirm_token/expires_at — all under `result.structuredContent`.
**Next up:** user sets `TEST_PHONE_NUMBER` in `.env`, runs `scripts/test_call.py`, reports outcome → log result here and flip Phase 2 if terminal status is success-ish.

### 2026-08-25 — Session 1
**Built:** git init (no commit); `app/config.py` (lazy env loading), `app/bot.py` (`/start`, `/help` + logging setup), `app/server.py` (`/healthz`), `app/__init__.py`, `.env.example`, `requirements.txt`, `.gitignore`; `.venv` created; deps pinned by latest stable install. Verified: token-less import smoke test, missing-token error path, `/healthz` 200 via curl+uvicorn, live polling run answering both commands in a real chat.
**Broke / bugs hit:** git-bash mangled backslash paths (`.venv\Scripts\python.exe`). Duplicate bot instances caused Telegram 409 Conflict ("terminated by other getUpdates request") — an earlier backgrounded launch survived a bash `kill`. Empty `bot.log` at first because PTB does not configure logging by default. Transient `httpx.ConnectError` blips mid-run (PTB retry loop auto-recovered). httpx logs embed the full bot token in request URLs.
**Fix / workaround:** forward-slash paths in bash; enumerated stray processes by `ExecutablePath` via PowerShell CIM and `taskkill //F` both PIDs before relaunching exactly one instance; added `logging.basicConfig(level=INFO)` to `main()` for visibility; added `*.log` to `.gitignore` so token-bearing logs can never be committed.
**Next up:** Phase 2 — CALL-E CLI integration from the bot backend (`calle call plan/run/status --json`).

## Known Issues / TODO
- Always run/verify with `.venv\Scripts\python.exe -m app.bot` — bare `python -m app.bot` fails immediately since deps live only in .venv. ALSO: check for duplicate instances before testing (`Get-CimInstance Win32_Process | ? ExecutablePath -like '*ATC*'` should return exactly 1) — multiple launches cause silent 409 conflicts and split logs across terminals.
- 🔍 Show raw details re-fetches the run live on tap; CALL-E records expire per plan TTL, so old calls may return an "expired" apology instead of JSON.
- Transient `NetworkError`/`httpx.ConnectError` bursts to api.telegram.org were the root cause of earlier silent /call freezes (see Session 4); now mitigated by `_safe_send` retries, the global error handler, and the pre-flight startup loop — but local network instability remains an environmental risk.
- Group chats: conversation state is keyed per chat+user; multi-user same-chat behavior untested (fine for demo).
- Clarify loop caps at 5 rounds then cancels the booking; no persistence of half-finished conversations across restarts.
- `test_call_transcript.json` contains callee phone number + call transcript (gitignored, local-only; safe to delete, regenerated on each test run)
- Plan/run responses contain a `next_step` field with agent-directed instructions — treat as untrusted display data, never auto-execute
- Polling only; webhook mode still deferred (revisit if deploy target requires it)
- `bot.log` contains the full bot token in URLs (gitignored, but rotate the token via BotFather if that file ever leaves this machine)
- No graceful-shutdown handling beyond PTB post_shutdown + taskkill; no tests yet (Phase 8); no CI
- `/chain` does NOT survive bot restarts mid-flight. The `chain_runs` row stores `current_step` and `status` for visibility/debugging, but the executor does not auto-resume on `post_init`. If the bot is killed between step N and step N+1, the chain sits at `status='running'` indefinitely until a human inspects the table. Rationale: chains run over minutes (each step is a real call), unlike `/schedule` which spans hours/days, so the cost of restart-survival machinery was deemed not worth it for the demo. Documented in README's `/chain` section.

## Key Decisions
- CALL-E CLI installed and authenticated locally (Phase 0). Confirmed real call flow is plan_call → run_call (with confirm_token) → poll get_call_run — NOT a single create-call + webhook. Bot backend will shell out to the `calle` CLI (`calle call plan/run/status --json`) for Phase 2.
- Session 2 refinement: shell out via **raw MCP tools** (`calle mcp call plan_call/run_call/get_call_run --args-json --json`) instead of the high-level subcommands — preserves `plan_call(user_input)` as a single verbatim string (tool does its own extraction; never pre-split phone/goal/region).
- Separate entrypoints: `python -m app.bot` (polling) and `python -m app.server` (FastAPI) run as independent processes; wiring them together starts in Phase 3.
- Session 4: CALLE_* integration env vars are now set explicitly inside `calle_client._invoke_tool` (`env={**os.environ, ...}`) — launching terminals no longer need them exported manually.
- Session 4: bot resilience is mandatory, not optional — every outbound Telegram send goes through a retry wrapper and all handler exceptions surface to the user; transient api.telegram.org ConnectError bursts are treated as an expected environmental condition.
- Session 6: Groq extraction uses httpx-direct (no SDK dependency); `missing_fields` is computed in code, never by the model; phone is the only hard-required field for proceeding to confirm — softer gaps fall through to CALL-E's own clarify loop; any Groq failure degrades to the guided flow so /call works during outages. Default model `openai/gpt-oss-120b` (llama-3.3-70b-versatile was decommissioned on Groq; 404 = unknown model).
- Session 7: emoji vocabulary is standardized (🔍 planning · 📞 in-progress · ✅ success · ⏰ scheduled · ❌ failed · ⚠️ warning · 🗑 user-cancelled) and each call owns ONE chat thread — launch message is edited in place through failures/results via `calls.status_message_id`; scheduled fires chain their ⏰ message into the same thread. Scheduling-time plan_call is validation-only (tokens never persisted; fire-time always re-plans fresh). APScheduler pinned to 3.x (4.x = breaking rewrite). Timezone limitation (demo scope): /schedule interprets times in the server machine's local timezone, assuming server and user share one timezone — also flagged in README.
- Session 10: /callaround reports EVERY clinic's outcome individually (no winner/suppression); CALL-E cannot cancel in-flight calls (verified), so losers run to completion but are still reported. Bootstrap supervision lives OUTSIDE the Python process (`run_bot_supervised.sh`) because PTB poisons module-level loop state after a failed run_polling on Python 3.14 — never retry run_polling in-process.
- Session 11: /earliest REUSES /callaround's launch path entirely — only the composed task input differs (asked by the user to mirror CALL-E's own display_goal style: "ask for the earliest available slot and report back without booking"). `_launch_multi_call` + `_ca_single_call` got a `mode: str = "book"` kw + a `_compose_task_input(mode, …)` dispatcher; the standard poller handles per-clinic reporting unchanged. New watcher (`earliest_watcher_loop`) is the only novel piece — fires only on `earliest_*` batch_ids once every row is terminal, then sorts + posts a single ranked summary. The preflight helper `_preflight_and_ask` was made mode-aware (branches on `user_data["mode"]`) so the same validate-before-confirm logic is shared; this also means future modes can be added with just one new state + one new entry point.
- Deferred stretch goal: earliest-slot comparison across a finished batch (e.g. "Clinic A can see you Tuesday, Clinic B Wednesday") — DELIVERED in Session 11 as the new `/earliest` command + `earliest_watcher_loop`; live multi-target test still pending.
- Session 13: `/chain` adds sequential-fallback booking. Simplifications applied per the user's clarifications: (a) the fallback trigger is a single rule — continue on anything other than `status == "COMPLETED" && task_completed: true`. No-answer, declined, voicemail, no-availability, "we don't do that" all trigger the next step identically; keyword/reason classification was dropped entirely; (b) quota mirrors `/callaround` exactly (N upfront, "Try first R steps" card when short, hard block at zero); (c) chain progress is stored in `chain_runs` for visibility only — restart-survival is explicitly out of scope because chains run in real time over minutes (unlike `/schedule`'s hours/days); (d) each chain runs a dedicated async function (`_execute_chain`) with its own plan → run → wait → decide loop, no shared-poller / Event coordination (chains don't interleave with concurrent calls). Step 1's `task_input` carries `BOOKING_ALTERNATIVES_INSTRUCTION` so the Session 12 callback-confirmation flow still applies to step 1; steps 2..N use a plain booking template. Each step's `calls` row carries `batch_id="chain_<short_id>"` so the standard poller still edits each step's "📞 Trying..." thread into the friendly summary — `_execute_chain` only needs the terminal `sc` to make the CONTINUE/STOP decision, not to render messages.

### 2026-09-05 — Session 15 (Phase W1 finish: extract /call, /schedule, /callaround, /earliest)

**Built:** Completed the channel-agnostic extraction for the four remaining flows that Session 13 left as bot-side helpers. Every flow now has the same shape as `/chain`: bot handlers are thin renderers that call `core.*` and dispatch the structured result; `app/core.py` owns all decision logic.

**Diff summary:**
- `app/bot.py`: 2812 → 2335 lines (-477, -17%). Top-level functions: 92 → 90 (-2; the 2 deletions were dead wrappers, replaced by thin in-handler code). Bot-side private helpers with `core.*` equivalents: 17 replaced with 1-line shims that forward to `core.*` (kept for any external test code that may import the old names). Live handlers now route through:
  - `/call`: `received_first`/`received_followup`/`received_who`/`received_what`/`on_confirm`/`received_clarify` → `core.start_call` / `core.call_clarify` / `core.call_collect_who` / `core.call_collect_what` / `core.call_confirm_card` / `core.call_plan_and_launch` / `core.call_clarify_post_confirm` / `core.attach_thread`. New thin wrapper: `_ask_confirm(chat_id, context)` wraps `core.call_confirm_card` for the language-prompt and inline-card dispatch.
  - `/schedule`: `sched_received` → `core.start_schedule` + `_run_sched_preflight` → `core.schedule_preflight`; `sched_followup` → `core.schedule_clarify` + `_run_sched_preflight`; `sched_plan_clarify` → `core.schedule_clarify_post_preflight`; `on_sched_confirm` → `core.schedule_job` (unchanged). New thin wrappers: `_ask_sched_confirm` (wraps `core.schedule_confirm_card`) and `_run_sched_preflight` (wraps `core.schedule_preflight`).
  - `/callaround`: `ca_received` / `ca_followup` / `ca_targets_collect` / `ca_what_collect` / `ca_clarify` / `on_ca_confirm` all already routed through `core.*`; this pass just fixed a minor bug in `on_ca_confirm` (the `n = min(n, quota.get("used", 0) + (quota["limit"] - quota.get("used", 0)))` line was a buggy self-cancellation — replaced with the correct `min(n, quota["limit"] - quota.get("used", 0))`) and removed the duplicate `_ca_keyboard_to_inline` (now lives once at the top of the bot file as a shared helper).
  - `/earliest`: identical pattern to `/callaround` (`mode="earliest"`); `early_received` / `early_followup` / `early_targets_collect` / `early_what_collect` / `early_clarify` / `on_early_confirm` already routed through `core.*`; no structural changes needed.
  - `/chain`: the Session 13 work extracted the executor + `on_chain_confirm`, but the bot-side **intake** (`chain_received` / `chain_followup`) still had all the Groq extraction, validation, and `_valid_chain_steps` / `_chain_compose_what` / `_preflight_chain` / `_ask_chain_confirm` inline. This pass routes both through `core.start_chain` / `core.chain_clarify_with_state` / `core.chain_preflight` / `core._ask_chain_confirm_payload`. Old helpers kept as shims for backwards compat.

**Broke / bugs hit:** None material — the existing four W1 parity test files (`test_w1_call.py` 54 tests, `test_w1_schedule.py` 50, `test_w1_callaround.py` 57, `test_w1_chain_alts.py` 52) all stayed green throughout the refactor, proving behavior preservation. The handler-level test (`test_w1_bot_handlers.py`, new in this session) needed ~10 minutes of debugging to work around a Python 3.10+ `with (...):` context-manager group edge case where the test runner's `__exit__` ran in a different scope than expected. The fix was tightening the `_FakeMessage` and `_FakeQuery` classes to match PTB's actual message and callback-query shape (`query.message.text` is a plain string attribute, not a `MagicMock` auto-attr, and the fake must carry real async `answer` and `edit_message_text` methods rather than relying on auto-attr Mocking).

**Fix / workaround:** as above.

**Verified:**
- `test_w1_call.py`: 54 / 54 pass
- `test_w1_schedule.py`: 50 / 50 pass
- `test_w1_callaround.py`: 57 / 57 pass (covers /callaround + /earliest)
- `test_w1_chain_alts.py`: 52 / 52 pass
- `test_w1_bot_handlers.py` (new, 31 tests): every bot handler correctly calls the expected `core.*` function with the expected args, persists the right state into `context.user_data`, and returns the right ConversationHandler state. Coverage spans `/call` (`received_first`, `received_followup`, `on_confirm` yes/no, `_ask_confirm` ready/needs_language), `/schedule` (`sched_received` ready/needs_clarify), `/callaround` (`ca_received` needs_clarify/ready), `/earliest` (`early_received` groq_error + mode set), `/chain` (`chain_received` ready/groq_unavailable, `on_chain_confirm` yes → core.start_chain_execution + _execute_chain scheduled).
- `ast.parse` succeeds on all three edited files (`pipeline.py` was not touched this session).
- **Total: 244 W1 tests, all green** (up from 213 before this session).

**Not verified:** Real end-to-end Telegram session for `/call`, `/callaround`, `/earliest`. The user has personally verified `/chain` and alternatives live (Session 13) and will personally verify `/schedule` end-to-end (including the fire-time path through the Channel registry) before final sign-off. The three remaining flows have parity + handler-smoke evidence; live verification per the user's earlier instruction is out of scope for this session.

**Architecture invariant now in place:** Every bot handler in `app/bot.py` follows one of two patterns — (a) a thin renderer that calls `core.<verb>(session_id, ...)` and dispatches the structured `result` dict to Telegram via `_safe_send` / `query.edit_message_text`; or (b) a long-running entry point (`post_init`, `poll_loop`, `earliest_watcher_loop`, `scheduled_call_job`, `_execute_chain`) that calls `core.register_channel(chat_id, _build_telegram_channel(bot, chat_id))` and then defers all message-sending to the channel. All user-visible strings (confirm-card text, "trying…" threads, friendly result messages, error messages) come from `app/core.py`; all PTB surface (PTB Update/ContextTypes, InlineKeyboardMarkup, ConversationHandler states) lives in `app/bot.py`. The 17 private helpers that were bot-side duplicates of core functions are kept as 1-line shims (`def _compose_what(d): return core.compose_what(d)`) so any future test or external code that imported them keeps working.

**Next up:** User to run live `/schedule` end-to-end in their real Telegram chat and confirm. After that, Part 2 (FastAPI routes in `app/server.py` + `WebChannel` + `POST /api/{call,schedule,callaround,chain,earliest}` + `GET /api/calls` + `GET /api/status/{id}` + curl-based smoke test).

### 2026-09-05 — Session 16
**Built:** `parse_when` lead-time hardening + debug log. Replaced the 30-second slop buffer (which was silently rejecting targets like "10:02 pm today" 9 minutes before the user thought, because Telegram/processing latency closed the gap) with a named constant `SCHEDULE_MIN_LEAD_S = 60` and an explanatory rejection message that includes both the picked time and the current server time (`"That time is too close — please pick a time at least a minute from now (you picked 22:02, it's now 22:01)."`). Added a `logger.info("parse_when: raw=… parsed=… now=… delta_s=… rejected=…")` line so future edge cases (TZ mismatches, dateparser behaviour changes, mid-sentence input) are visible in the log without guessing. Decoupled from APScheduler's own `misfire_grace_time=300` in `bot.py` — that handles a different concern (a job that fires late) and stays untouched.

**Broke / bugs hit:** None — the 4 existing `parse_when` tests stayed green after updating the loose "past in err" assertion in `test_parse_when_past_time` to a behaviour-contract assertion (resolved=None + err non-empty; the wording changed but the function still rejects past times).

**Verified:** `scripts/test_w1_schedule.py` — 52 → 56 tests, all green:
- modified `test_parse_when_past_time` (still asserts resolved=None + err non-empty)
- new `test_parse_when_within_60s_rejected` (locks in the 60s boundary: target 30s in future is rejected; under the old 30s slop, the same input would have been accepted)
- new `test_parse_when_too_close_message_includes_times` (asserts on "picked" + "it's now" substrings — option ii, not exact HH:MM values, to keep the test stable against wall-clock timing)
- full W1 suite: **250/250 passing** (was 244, +6: 54 /call + 57 /callaround+/earliest + 56 /schedule + 52 chain/alts + 31 bot-handler smoke)
- manual live reproduction: at 22:07 IST, `parse_when("10:02 pm today")` returns `(None, "That time is too close — please pick a time at least a minute from now (you picked 22:02, it's now 22:07).")` and the debug line appears in stdout with the full `parsed=… now=… delta_s=… rejected=…` values.

**Not verified:** live `/schedule` end-to-end for the original job_id collision bug — that's a separate sign-off the user is running after this session, now that this timing edge case is no longer confounding the result.

**Next up:** Phase 8 — live `/schedule` re-test for the job_id / insert_scheduled double-insert symptom (the W1.1 fix in `app/core.py` `schedule_job` is already in place; this re-test is to confirm the live symptom no longer reproduces), then consolidated test harness, demo recording, submission.

### 2026-09-05 — Session 17
**Built:** Two W1 extraction regressions fixed in one pass.

1. **`NameError: name '_mask_phones_in' is not defined` in `/mycalls`.** The function had been relocated to `app/masking.py` as `mask_phones_in` (no underscore prefix) during the W1 split, but `app/bot.py` had two stale call sites (line 504, line 809) and a stale comment at line 229 referencing the old name with no import. Added `from app.masking import mask_phones_in` to bot.py, renamed the two call sites, and updated the stale comment. `grep -rn "_mask_phones_in" app/ scripts/` now returns zero hits.

2. **`patient_name` dropped on the `/schedule` fire-time path.** `patient_name` was wired into `/call`, `/callaround`, `/earliest`, and `/chain` (Groq extracts it, `_apply_details` stores it in state, every fire path threads it into the composed CALL-E task text), but the `/schedule` path captured it at intake then silently dropped it at three points: never written to the `scheduled_calls` table, never passed to the APScheduler job's `args`, and never threaded into `fire_scheduled_call` (both `_compose_book_task_input(..., patient_name=None)` and `launch_call(..., patient_name=None)` were hardcoded `None`). This is much worse for `/schedule` than for `/call` because the scheduled call fires unattended — if CALL-E re-asks the patient name at fire time, nobody's there to answer and the call aborts. Fix:
   - `app/db.py`: added `patient_name TEXT` to `_SCHEDULED_SCHEMA`; added a `_migrate()` block (`PRAGMA table_info` + idempotent `ALTER TABLE … ADD COLUMN`, same pattern as the existing `calls.patient_name` migration); extended `insert_scheduled(...)` signature with `patient_name: str | None = None`; extended `list_scheduled_for_chat` SELECT to include the new column.
   - `app/core.py` `schedule_job`: reads `state.get("patient_name")`, passes it to both `add_job_fn(..., patient_name=patient_name)` and `db.insert_scheduled(..., patient_name)`.
   - `app/core.py` `fire_scheduled_call`: added `patient_name: str | None = None` parameter; `_compose_book_task_input(who, what, patient_name=patient_name)` and `launch_call(..., patient_name=patient_name)` now use the threaded value.
   - `app/bot.py` `_add_job` closure: signature `(*, job_id, chat_id, who, what, language, patient_name=None)`, `args=[job_id, chat_id, who, what, language, patient_name]` so APScheduler pickles the name.
   - `app/bot.py` `scheduled_call_job`: added `patient_name: str | None = None` param, forwards to `core.fire_scheduled_call(...)`. The new param is keyword-style with a default of `None`, so any pre-existing persisted APScheduler job that was serialised with the old 5-positional-arg layout still loads cleanly (APScheduler pickles `args` as a list and re-passes them positionally; the new tail-position kwarg with default keeps it working).

**Defaults agreed in advance (per user's call before I executed):** historical `scheduled_calls` rows are left as `NULL` (retroactive fill is not possible and not useful — they were `fired`/`failed` historicals from the original test sessions); `patient_name` is NOT shown in `/mycalls` output (the field doesn't belong in a status listing); `patient_name` is NOT added to the confirm card (mirrors the existing `/call`/`/callaround`/`/earliest`/`/chain` behaviour — it's captured but not shown until the result message).

**Broke / bugs hit:**
- W1.2 test `test_fire_scheduled_call_omits_patient_name_when_none` — my first assertion tried to assert `" for " not in user_input` and failed because the substring appears in the SAFETY_SUFFIX ("Only check availability, **for** book or change…"). Fixed by tightening the assertion to check only the first clause (the "Call …" segment before the first `. `).
- W1.2 test `test_on_sched_confirm_patient_name_threaded_through_to_scheduler` — the `fake_schedule_job` mock was a plain `def` but the bot's `await core.schedule_job(...)` needs a coroutine. Fixed by making it `async def`. The `fake_add_job` also needed the proper `_scheduler.add_job(func, trigger, args, id, name, ...)` signature with `**kwargs`.

**Verified:**
- `grep -rn "_mask_phones_in" app/ scripts/` → 0 hits
- `app.bot.mask_phones_in` is callable (imported from `app.masking`)
- All 3 signatures confirmed: `db.insert_scheduled(chat_id, job_id, run_at, who, what, language, patient_name)`, `core.fire_scheduled_call(job_id, chat_id, who, what, language, patient_name)`, `bot.scheduled_call_job(job_id, chat_id, who, what, language, patient_name)` — each with `patient_name: str | None = None` as the trailing default
- `scheduled_calls` schema after live migration: `id, chat_id, job_id, run_at, status, who, what, created_at, language, patient_name` (column added in 2nd-to-last position by ALTER)
- Idempotency: `ALTER TABLE scheduled_calls ADD COLUMN patient_name TEXT` raises `sqlite3.OperationalError: duplicate column name: patient_name` on a re-run; the `_migrate()` block catches it via `OperationalError` and continues (same pattern as the other columns in the same block)
- All W1 suites: **259/259 passing** (was 250, +9):
  - `test_w1_call.py`: 54 / 54
  - `test_w1_callaround.py`: 57 / 57
  - `test_w1_chain_alts.py`: 52 / 52
  - `test_w1_schedule.py`: 62 / 62 (was 56, +6: 2 modified for patient_name persistence, 1 new for `test_fire_scheduled_call_composes_task_with_patient_name`, 1 new for `test_fire_scheduled_call_omits_patient_name_when_none`, 1 new for the `parse_when` slop regression from Session 16 was already counted)
  - `test_w1_bot_handlers.py`: 34 / 34 (was 31, +3 from new `test_on_sched_confirm_patient_name_threaded_through_to_scheduler`)

**Not verified:** Live end-to-end `/schedule` re-test for the original job_id/insert_scheduled double-insert symptom (the W1.1 fix is in place; this is the next live sign-off the user is running). No DB cleanup was needed: 0 rows in `apscheduler_jobs`, 0 `scheduled` rows in `scheduled_calls`, no orphans.

**Next up:** Live `/schedule` re-test (job_id collision symptom + new patient_name threading) — to confirm one scheduled call produces one APScheduler job, one DB row, and a fire-time task that includes the patient name when one was provided.

### 2026-09-06 — Session 18
**Built:** Terminal-status normalisation + poller safety net. CALL-E has been observed returning both `"NO_ANSWER"` (underscore) and `"NO ANSWER"` (space) for the same logical outcome. `TERMINAL_STATUSES` and the per-status text/icon dicts (`FAILURE_TEXTS`, `_friendly_status`) used the underscore form, so the poller's `if status in TERMINAL_STATUSES:` check at `app/bot.py:1537` never matched the space form, leaving calls stuck in `status='running'` forever. Confirmed by a live stuck row: `id=312, run_id='I0YG1Vdcpm4-b9JrhtsaDw', status='running', created_at='2026-09-06 08:38:28'` was the exact failure mode.

Fix in 4 layers:

1. **`app/calle_client.py` — new canonicalisation helpers** (`_normalize_status`, `normalize_status`, `is_terminal`) that upper-case, trim, and fold spaces to underscores before any comparison. `TERMINAL_STATUSES` collapsed to a single canonical spelling for each logical status (`CANCELED` and `CANCELLED` collapsed to one key — CALL-E has returned both, and the W1 rule is single source of truth).

2. **All `if status in TERMINAL_STATUSES` checks** swapped to `if is_terminal(status):` at three sites: `app/bot.py:1537` (the poller), `app/core.py:786` (the chain executor's 1200-iteration polling loop), and `scripts/test_call.py:106` (the manual test script). The chain fix is the most impactful — without it, a chain step reported as `"NO ANSWER"` would spin for the full 60 minutes before timing out.

3. **All dict-lookups-by-status** normalised at the lookup site: `app/bot.py:1335` (`FAILURE_TEXTS.get(...)` in `_friendly_result_text`) and `app/bot.py:1895` (`_friendly_status`). Removes the silent fallback to the default message when the key didn't match. Also: `db.finish_call(run_id, status)` now writes the **canonical** form (e.g. `"NO_ANSWER"` even if CALL-E returned `"NO ANSWER"`) so the `calls.status` column is consistent across the DB.

4. **Poller safety net** at the bottom of every `poll_loop` tick. New constant `MAX_POLL_DURATION_SECONDS = 300` (5 min). New module-level `_last_status_cache: dict[str, str]` (lifetime matches `_error_counts`) tracks the last raw status from CALL-E for each run_id, so the safety-net warning log includes diagnostic context. New `db.get_stuck_running_calls(older_than_seconds)` query returns rows stuck in `running` for longer than the bound. The pass:
   - Logs a `POLL_SAFETY` warning (loud — `logger.warning` with full chat_id, call row id, run_id, last-known status)
   - Force-finishes the row as `POLL_ERROR` via `db.finish_call`
   - Sends the user a "⚠️ I lost track of your call after 5 minutes — its status is unclear. Please check with the clinic directly" message
   - Cleans up `_last_status_cache` and `_error_counts`
   - The whole pass is wrapped in its own try/except so a bug in the safety-net itself doesn't kill the poller
   The safety-net query uses `strftime('%Y-%m-%d %H:%M:%S', created_at) < strftime('%Y-%m-%d %H:%M:%S', 'now', 'localtime', '-300 seconds')` — the `strftime` normalisation handles the `'T'` vs `' '` separator mismatch between Python's `isoformat()` (used in DB inserts) and SQLite's `datetime('now')` (used in the comparison), and `'localtime'` handles the IST vs UTC timezone mismatch. Without this, a 6-minute-old row (local) was matching as "5h36m old" or "1m14s old" depending on direction — both wrong.

5. **Left the live `id=312` row alone in the DB** per agreement, so the safety net proves itself naturally: on the next bot restart, the first eligible tick sees the row (verified with `db.get_stuck_running_calls(300)` — it returns `(id=312, chat=1110569418, run_id='I0YG1Vdcpm4-b9JrhtsaDw', clinic='Dr Sharma Dental Clinic', patient='Kapil', created_at='2026-09-06 08:38:28')`) and force-reports it as `POLL_ERROR`.

**Broke / bugs hit:**
- W1.2 test `test_fire_scheduled_call_omits_patient_name_when_none` — first assertion tried to assert `" for " not in user_input`; failed because the substring appears in the SAFETY_SUFFIX ("Only check availability, **for** book or change…"). Tightened to check only the first clause (the "Call …" segment before the first `. `). Fixed in Session 17, noted here for completeness.
- W1.2 test `test_on_sched_confirm_patient_name_threaded_through_to_scheduler` (bot-handlers) — `fake_schedule_job` was a plain `def` but the bot's `await core.schedule_job(...)` needs a coroutine. Fixed by making it `async def`. The `fake_add_job` also needed the proper `_scheduler.add_job(func, trigger, args, id, name, ...)` signature with `**kwargs`. Session 17.
- Session 18 test `test_poller_safety_net_force_reports_stuck_row` — first version of `get_stuck_running_calls` used `datetime('now', '-300 seconds')` (UTC), but `created_at` is written in local time (naive IST). Result: a 6-min-old row was either "5h36m old" (matched everything) or "1m14s old" (matched nothing), both wrong. Fixed by switching to `strftime(..., 'localtime', ...)`. Same for the `'T'` vs `' '` separator mismatch — Python's `isoformat()` writes `2026-09-06T14:19:41`, SQLite's `datetime('now')` writes `2026-09-06 14:20:55`; string comparison failed lex because `T` (0x54) > ` ` (0x20). `strftime` on both sides gives a clean ISO-with-space comparison.
- Session 18 test `test_execute_chain_recognises_no_answer_with_space` — first version asserted `calls.status='NO_ANSWER'` for the chain flow, but the chain executor never calls `db.finish_call`; it manages chain progress via `chain_runs.status` only. Rewrote the test to assert (1) the mock was polled exactly once (not 1200) and (2) `chain_runs.status='failed'` (chain reached the no-more-steps branch after the failed step).

**Verified:**
- `grep -rn '"NO_ANSWER"\|"NO ANSWER"' app/ scripts/` → app code uses only the canonical underscore form (3 hits, all intentional: calle_client TERMINAL_STATUSES, bot.py FAILURE_TEXTS, bot.py _friendly_status). Test files use both forms intentionally to verify normalisation.
- `TERMINAL_STATUSES` collapsed: `{'BUSY', 'CANCELLED', 'COMPLETED', 'DECLINED', 'EXPIRED', 'FAILED', 'NO_ANSWER', 'VOICEMAIL'}` — no `CANCELED`, no `POLL_ERROR` (it's a project-internal marker, not a CALL-E status).
- Live check: `db.get_stuck_running_calls(300)` returns the live `id=312` row exactly as expected → safety net will fire on the next bot tick that crosses 5 min from its `created_at`.
- All 5 W1 suites: **276/276 passing** (was 259, +17):
  - `test_w1_bot_handlers.py`: 40 / 40 (was 34, +6: 3 from new `test_poll_loop_recognises_no_answer_with_space` and 3 from new `test_friendly_status_normalises_no_answer_with_space`)
  - `test_w1_call.py`: 63 / 63 (was 54, +9: 7 from new `test_normalize_status_and_is_terminal` and 2 from new `test_poller_safety_net_force_reports_stuck_row`)
  - `test_w1_callaround.py`: 57 / 57
  - `test_w1_chain_alts.py`: 54 / 54 (was 52, +2 from new `test_execute_chain_recognises_no_answer_with_space`)
  - `test_w1_schedule.py`: 62 / 62
- `ast.parse` succeeds on all 6 modified files.

**Not verified:** Live end-to-end — the safety net has not yet fired on the live `id=312` row (waiting for the user to restart the bot and wait for the 5-min window from `created_at='2026-09-06 08:38:28'`). The first eligible tick after restart will produce a `POLL_SAFETY` warning log entry and a "status unclear" message to chat 1110569418.

**Next up:** Restart the bot and watch `bot.log` for the next 5+ minutes. The safety net should fire on the live `id=312` row, log a `POLL_SAFETY` warning, mark the row as `POLL_ERROR`, and send a "status unclear" message to the user. Then place a fresh `/call` to a number CALL-E will mark as `NO_ANSWER` to confirm the space-form terminal status is now correctly recognised end-to-end (user gets the "no one answered" message within ~5-10s, row gets the canonical `NO_ANSWER` status).

### 2026-09-06 — Session 18 follow-up
**Built:** Safety-net timezone fix + diagnostic log + regression tests. Live testing found the Session 18 safety net was firing on freshly-inserted calls (rows 378 and 379 were force-reported as `POLL_ERROR` after ~7-23 real seconds, while their `created_at` was 5h35m earlier in wall-clock terms than the threshold the bot was computing). Session 18 was wrong about `calls.created_at` being local time; it's actually written by SQLite's `DEFAULT (datetime('now'))`, which is **UTC**. The safety query used `'now', 'localtime', '-300 seconds'` for the threshold (local IST, minus 5 min). The lexicographic comparison between a UTC `created_at` and an IST-5min threshold always matched because UTC is 5h30m behind IST — so any row written between local 00:00:00 and local 14:38:29 looked "older than 5 min" to the bot.

Fix (γ + α — minimal):
1. **`app/db.py:217`** — dropped the `'localtime'` modifier from the safety query's threshold. One-line change. Updated the docstring to reflect the actual timezone (`created_at` is UTC, not local).
2. **`app/db.py:223-234`** — new helper `utc_now_iso() -> str` that returns `strftime('%Y-%m-%d %H:%M:%S', 'now')` (UTC). Used by the poller to compute a real `elapsed_s` in Python next to each stuck row.
3. **`app/bot.py:1571-1632`** — per-tick `logger.info("POLL_SAFETY pass: now_utc=%s threshold_s=%d stuck_rows=%d", ...)` so every poller tick logs the safety-net's inputs once (even when the result is empty). For each force-reported row, the warning log now prints the raw `created_at` (from the DB row), the raw `now_utc` (from `db.utc_now_iso()`), and the **real** `elapsed_s` (computed in Python as `(datetime.fromisoformat(now_utc) - datetime.fromisoformat(srow["created_at"])).total_seconds()`), so a future timezone mismatch surfaces immediately in `bot.log` instead of as a silently-firing safety net.
4. **`scripts/test_w1_call.py`** — 2 new regression tests:
   - `test_safety_net_fresh_insert_not_matched`: a row inserted via `db.insert_call(...)` and queried immediately is NOT returned by `get_stuck_running_calls(300)`. Locks in the half of the fix that protects in-progress calls.
   - `test_safety_net_six_minute_old_row_is_matched`: a row whose `created_at` is back-dated 6 minutes IS returned. Locks in the other half — the safety net still fires on a genuinely stuck row.
   Both use `chat_id=88888` and clean up after themselves.
5. **`scripts/test_w1_call.py:test_poller_safety_net_force_reports_stuck_row`** — fixed the Session 18 back-date helper (it used `datetime.now() - timedelta(minutes=6)`, which was local time; now uses `datetime.utcnow() - timedelta(minutes=6)` to match the actual `created_at` timezone and the safety query's threshold).

**Deliberately NOT fixed:** `scheduled_calls.created_at`, `chain_runs.created_at`, `pending_confirmations.created_at` all use the same `DEFAULT (datetime('now'))` (UTC) pattern. Nothing currently queries those `created_at` columns against a wall-clock threshold, so the latent inconsistency is unexploited. If a future feature adds a time-threshold query against any of those tables, the same bug will recur — fix then by either (a) changing the schema default to `datetime('now', 'localtime')` via an `ALTER TABLE … RENAME TO …__old; CREATE TABLE …; INSERT …; DROP TABLE …__old` rebuild, or (b) using `'now'` (UTC) in the threshold to match what `created_at` is actually written in.

**Not verified live:** the 5-min real-time wait test was skipped per user request — the unit tests prove the same SQL comparison deterministically without burning 5 real minutes or making a real call. The two regression tests are the load-bearing sign-off.

**Verified:**
- `grep -n "localtime" app/db.py` → 0 hits (the broken modifier is gone)
- Fresh insert via `db.insert_call(88888, 'p', 'r')` immediately followed by `db.get_stuck_running_calls(300)` returns `[]` (no false-positive on a 15ms-old row)
- `db.utc_now_iso()` returns a UTC timestamp like `2026-09-06 10:00:05` (matches SQLite's `datetime('now')` output)
- All 5 W1 suites: **280/280 passing** (was 276, +4 from the 2 new regression tests):
  - `test_w1_call.py`: 67 / 67 (was 63, +4: 2 from `test_safety_net_fresh_insert_not_matched`, 2 from `test_safety_net_six_minute_old_row_is_matched`)
  - `test_w1_bot_handlers.py`: 40 / 40
  - `test_w1_callaround.py`: 57 / 57
  - `test_w1_chain_alts.py`: 54 / 54
  - `test_w1_schedule.py`: 62 / 62
- `ast.parse` succeeds on all 3 modified files.

**Next up:** Restart the bot. Watch `bot.log` — every poller tick should now print `POLL_SAFETY pass: now_utc=… threshold_s=300 stuck_rows=0` (or higher if there's a genuinely stuck row). Place a fresh `/call` to confirm the safety net does not force-report it after 7-23 seconds; the call should complete normally via the terminal-status path.
